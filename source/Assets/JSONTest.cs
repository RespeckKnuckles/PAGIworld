using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*[System.Serializable]
public class testClass {
	public string name;
	public string id;
}*/

public class JSONTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//testClass t = new testClass(); 
		//t.name = "Austin";
		//t.id = "656";
		//string jstr = JsonUtility.ToJson (t);
		//Debug.Log (jstr);

		// open the testClass.json file and see if we can create an object out of it


		// create a test object
		//AIMessage test = new AIMessage("print", "hello world!");

		// create a JSON object from the test object
		//string jsonStr = JsonUtility.ToJson(test);
		//Debug.Log (jsonStr);

		// create an AIMessage object from the json string
		//AIMessage test2 = AIMessage.createFromJSON(jsonStr);

		// print the values of the test2 object to check for consistency

		//test = test.createFromJSON(blah);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
