using UnityEngine;
using System.Collections;

public class garfBigScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy(gameObject, 20f); //self destruct after 20 seconds
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
